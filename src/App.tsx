import React from "react";
import { Button, Text, View } from "react-native";

const App = () => {
  return (
    <View >
      <Button onPress={() => {}} title="Example button" />
    </View>
  );
}

export default App;
